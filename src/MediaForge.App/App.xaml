<Application x:Class="MediaForge.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:MediaForge.App.Converters"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Light"/>
                <ui:ControlsDictionary/>
                <ResourceDictionary Source="pack://application:,,,/MediaForge;component/Localization/Languages.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <conv:BooleanToVisibilityConverterFallback x:Key="BooleanToVisibilityConverterFallback"/>
            <conv:BooleanNotConverter x:Key="BooleanNotConverter"/>
            <conv:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
            <conv:DirectoryGlyphConverter x:Key="DirectoryGlyphConverter"/>
            <conv:StringEqualsConverter x:Key="StringEqualsConverter"/>

            <SolidColorBrush x:Key="MediaForgeBackgroundBrush" Color="#F4F7FB"/>
            <SolidColorBrush x:Key="MediaForgeSurfaceBrush" Color="#FFFFFF"/>
            <SolidColorBrush x:Key="MediaForgeSurfaceSoftBrush" Color="#F8FAFD"/>
            <SolidColorBrush x:Key="MediaForgeBorderBrush" Color="#E3EAF2"/>
            <SolidColorBrush x:Key="MediaForgeTextBrush" Color="#152238"/>
            <SolidColorBrush x:Key="MediaForgeMutedBrush" Color="#708096"/>
            <SolidColorBrush x:Key="MediaForgeAccentBrush" Color="#2563EB"/>
            <SolidColorBrush x:Key="MediaForgeAccentSoftBrush" Color="#EDF4FF"/>
            <SolidColorBrush x:Key="MediaForgePendingBrush" Color="#B77900"/>
            <SolidColorBrush x:Key="MediaForgePendingSoftBrush" Color="#FFF8E8"/>
            <SolidColorBrush x:Key="MediaForgeSuccessBrush" Color="#22945F"/>
            <SolidColorBrush x:Key="MediaForgeErrorBrush" Color="#C73D54"/>
            <SolidColorBrush x:Key="MediaForgeDangerSoftBrush" Color="#FFF1F4"/>
            <LinearGradientBrush x:Key="MediaForgeAccentGradient" StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#27D8F4" Offset="0"/>
                <GradientStop Color="#2A72FF" Offset="0.52"/>
                <GradientStop Color="#A341FF" Offset="1"/>
            </LinearGradientBrush>

            <DrawingImage x:Key="MediaForgeIconImage">
                <DrawingImage.Drawing>
                    <DrawingGroup>
                        <GeometryDrawing Brush="#07122D">
                            <GeometryDrawing.Geometry>
                                <RectangleGeometry Rect="0,0,64,64" RadiusX="14" RadiusY="14"/>
                            </GeometryDrawing.Geometry>
                        </GeometryDrawing>
                        <GeometryDrawing>
                            <GeometryDrawing.Pen>
                                <Pen Thickness="8" LineJoin="Round" Brush="{StaticResource MediaForgeAccentGradient}"/>
                            </GeometryDrawing.Pen>
                            <GeometryDrawing.Geometry>
                                <PathGeometry>
                                    <PathFigure StartPoint="13,47">
                                        <LineSegment Point="13,21"/>
                                        <LineSegment Point="32,39"/>
                                        <LineSegment Point="51,21"/>
                                        <LineSegment Point="51,47"/>
                                    </PathFigure>
                                </PathGeometry>
                            </GeometryDrawing.Geometry>
                        </GeometryDrawing>
                        <GeometryDrawing Brush="{StaticResource MediaForgeAccentGradient}">
                            <GeometryDrawing.Geometry>
                                <PathGeometry>
                                    <PathFigure StartPoint="26,39" IsClosed="True">
                                        <LineSegment Point="26,50"/>
                                        <LineSegment Point="40,44.5"/>
                                    </PathFigure>
                                </PathGeometry>
                            </GeometryDrawing.Geometry>
                        </GeometryDrawing>
                    </DrawingGroup>
                </DrawingImage.Drawing>
            </DrawingImage>

            <Style TargetType="{x:Type Window}">
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Foreground" Value="{StaticResource MediaForgeTextBrush}"/>
            </Style>

            <Style x:Key="MediaForgePrimaryButtonStyle" TargetType="Button">
                <Setter Property="MinHeight" Value="42"/>
                <Setter Property="Padding" Value="18,0"/>
                <Setter Property="Background" Value="{StaticResource MediaForgeAccentBrush}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="FontSize" Value="12.5"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="ButtonBorder"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="11"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="#1D4ED8"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="#1E40AF"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.42"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="MediaForgeSubtleButtonStyle" TargetType="Button">
                <Setter Property="MinHeight" Value="42"/>
                <Setter Property="Padding" Value="16,0"/>
                <Setter Property="Background" Value="#FFFFFF"/>
                <Setter Property="Foreground" Value="#334155"/>
                <Setter Property="BorderBrush" Value="#D6E0EA"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="ButtonBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="11"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="#F6F9FC"/>
                                    <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#C3D0DF"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="#EDF2F8"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.42"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="MediaForgeDangerButtonStyle" TargetType="Button" BasedOn="{StaticResource MediaForgeSubtleButtonStyle}">
                <Setter Property="Foreground" Value="{StaticResource MediaForgeErrorBrush}"/>
                <Setter Property="Background" Value="{StaticResource MediaForgeDangerSoftBrush}"/>
                <Setter Property="BorderBrush" Value="#F0CBD2"/>
            </Style>

            <Style x:Key="MediaForgeNavButtonStyle" TargetType="Button">
                <Setter Property="Height" Value="40"/>
                <Setter Property="MinWidth" Value="98"/>
                <Setter Property="Padding" Value="14,0"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="#708096"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="NavBorder" Background="{TemplateBinding Background}" CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="NavBorder" Property="Background" Value="#F4F7FB"/>
                                    <Setter Property="Foreground" Value="#30425A"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="NavBorder" Property="Background" Value="#EAF0F7"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.45"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="MediaForgeInputStyle" TargetType="TextBox">
                <Setter Property="MinHeight" Value="42"/>
                <Setter Property="Padding" Value="13,0"/>
                <Setter Property="Background" Value="#FFFFFF"/>
                <Setter Property="Foreground" Value="{StaticResource MediaForgeTextBrush}"/>
                <Setter Property="BorderBrush" Value="#D5DEE9"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="FontSize" Value="12.5"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border x:Name="InputBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <ScrollViewer Margin="{TemplateBinding Padding}"
                                              x:Name="PART_ContentHost"
                                              VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="InputBorder" Property="BorderBrush" Value="#78A7F9"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="InputBorder" Property="Opacity" Value="0.55"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="MediaForgeInlineInputStyle" TargetType="TextBox" BasedOn="{StaticResource MediaForgeInputStyle}">
                <Setter Property="MinHeight" Value="36"/>
                <Setter Property="Padding" Value="9,0"/>
                <Setter Property="FontSize" Value="12.5"/>
            </Style>

            <Style x:Key="MediaForgePathBoxStyle" TargetType="TextBox" BasedOn="{StaticResource MediaForgeInputStyle}">
                <Setter Property="Background" Value="#F7FAFD"/>
                <Setter Property="BorderBrush" Value="#DCE5EE"/>
                <Setter Property="Padding" Value="11,0"/>
                <Setter Property="FontSize" Value="11.5"/>
            </Style>

            <Style x:Key="MediaForgeComboStyle" TargetType="ComboBox">
                <Setter Property="Height" Value="42"/>
                <Setter Property="Padding" Value="10,0"/>
                <Setter Property="Background" Value="#FFFFFF"/>
                <Setter Property="Foreground" Value="{StaticResource MediaForgeTextBrush}"/>
                <Setter Property="BorderBrush" Value="#D5DEE9"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="FontSize" Value="11.5"/>
            </Style>

            <Style x:Key="MediaForgeCardStyle" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource MediaForgeSurfaceBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource MediaForgeBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="16"/>
            </Style>

            <Style x:Key="MediaForgeSectionTitleStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="17"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{StaticResource MediaForgeTextBrush}"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
