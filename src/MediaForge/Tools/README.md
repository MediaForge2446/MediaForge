# MediaForge Tools

Place the runtime executables used by the download engine in this directory:

- `yt-dlp.exe`
- `ffmpeg.exe`

The application validates both files before starting a physical download.
Do not commit the executable binaries to the repository.
