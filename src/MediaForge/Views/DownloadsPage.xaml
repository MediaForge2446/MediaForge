<Page
    x:Class="MediaForge.Views.DownloadsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:MediaForge.Converters">
    <Page.Resources>
        <converters:ChangeStatusToBrushConverter x:Key="ChangeStatusBrushConverter" />
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:FailedStatusToVisibilityConverter x:Key="FailedStatusToVisibilityConverter" />
    </Page.Resources>

    <Grid Padding="28" RowSpacing="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid ColumnSpacing="14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Spacing="4">
                <TextBlock Text="Downloads" Style="{StaticResource PageTitleTextStyle}" />
                <TextBlock Text="Monitor staged downloads and completed media without leaving the workspace."
                           Style="{StaticResource PageSubtitleTextStyle}" />
            </StackPanel>
            <Button Grid.Column="1"
                    Content="Open Explorer"
                    Click="OnOpenExplorerClick"
                    Style="{StaticResource PrimaryToolbarButtonStyle}"
                    VerticalAlignment="Bottom" />
        </Grid>

        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>
            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel Spacing="3">
                    <TextBlock Text="{Binding ViewModel.PendingCount}" FontSize="26" FontWeight="SemiBold" />
                    <TextBlock Text="Pending" Style="{StaticResource CaptionTextStyle}" />
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}">
                <StackPanel Spacing="3">
                    <TextBlock Text="{Binding ViewModel.OverallProgressText}" FontSize="26" FontWeight="SemiBold" />
                    <TextBlock Text="Current progress" Style="{StaticResource CaptionTextStyle}" />
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
                <StackPanel Spacing="3">
                    <TextBlock Text="Staged first" FontSize="20" FontWeight="SemiBold" />
                    <TextBlock Text="Downloads are committed only when you save changes."
                               Style="{StaticResource CaptionTextStyle}" />
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}">
            <Grid RowSpacing="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="2*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Queue" Style="{StaticResource SectionTitleTextStyle}" />
                    <ProgressBar Grid.Column="1"
                                 Maximum="1"
                                 Value="{Binding ViewModel.OverallProgress}"
                                 Visibility="{Binding ViewModel.IsSaving, Converter={StaticResource BoolToVisibilityConverter}}" />
                </Grid>
                <ListView Grid.Row="1"
                          ItemsSource="{Binding ViewModel.Changes}"
                          SelectionMode="None">
                    <ListView.Resources>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Padding" Value="0" />
                        </Style>
                    </ListView.Resources>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Padding="12" ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="12" />
                                    <ColumnDefinition Width="88" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="160" />
                                </Grid.ColumnDefinitions>
                                <Ellipse Width="9" Height="9" VerticalAlignment="Center"
                                         Fill="{Binding Status, Converter={StaticResource ChangeStatusToBrushConverter}}" />
                                <TextBlock Grid.Column="1" Text="{Binding Type}" FontWeight="SemiBold" VerticalAlignment="Center" />
                                <StackPanel Grid.Column="2" Spacing="3">
                                    <TextBlock Text="{Binding TargetPath}" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{Binding StatusText}" Style="{StaticResource CaptionTextStyle}" />
                                    <ProgressBar Maximum="1"
                                                 Value="{Binding Progress}"
                                                 Visibility="{Binding ShowProgress, Converter={StaticResource BoolToVisibilityConverter}}" />
                                    <TextBlock Text="{Binding ErrorMessage}"
                                               Foreground="{StaticResource FailedStatusBrush}"
                                               TextWrapping="Wrap"
                                               Visibility="{Binding Status, Converter={StaticResource FailedStatusToVisibilityConverter}}" />
                                </StackPanel>
                                <TextBlock Grid.Column="3"
                                           Text="{Binding SourcePath}"
                                           Style="{StaticResource CaptionTextStyle}"
                                           TextWrapping="Wrap"
                                           VerticalAlignment="Center" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Page>
