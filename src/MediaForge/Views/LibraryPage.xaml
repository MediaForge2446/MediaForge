<Page
    x:Class="MediaForge.Views.LibraryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Padding="32" RowSpacing="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid ColumnSpacing="16">
                <StackPanel Spacing="6">
                    <TextBlock Text="Library" Style="{StaticResource PageTitleTextStyle}" />
                    <TextBlock Text="Your media roots, organized for fast browsing and staged updates."
                               Style="{StaticResource PageSubtitleTextStyle}" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                    <Button Style="{StaticResource ToolbarButtonStyle}"
                            Content="Add folder"
                            Click="OnAddFolderClick" />
                    <Button Style="{StaticResource PrimaryToolbarButtonStyle}"
                            Content="Add media"
                            Click="OnAddMediaClick" />
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" ColumnSpacing="12">
                <Border Style="{StaticResource CardBorderStyle}">
                    <StackPanel Spacing="4">
                        <TextBlock Text="{Binding ViewModel.RootFolders.Count}"
                                   FontSize="28"
                                   FontWeight="SemiBold" />
                        <TextBlock Text="Library folders" Style="{StaticResource CaptionTextStyle}" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}">
                    <StackPanel Spacing="4">
                        <TextBlock Text="{Binding ViewModel.Main.State.PendingChanges.PendingCount}"
                                   FontSize="28"
                                   FontWeight="SemiBold" />
                        <TextBlock Text="Pending changes" Style="{StaticResource CaptionTextStyle}" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
                    <StackPanel Spacing="4">
                        <TextBlock Text="Ready"
                                   FontSize="20"
                                   FontWeight="SemiBold" />
                        <TextBlock Text="Everything stays staged until Save Changes."
                                   Style="{StaticResource CaptionTextStyle}" />
                    </StackPanel>
                </Border>
            </Grid>

            <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}" Padding="18">
                <StackPanel Spacing="4">
                    <TextBlock Text="Library roots" Style="{StaticResource SectionTitleTextStyle}" />
                    <TextBlock Text="Double-click a folder to open it in Explorer."
                               Style="{StaticResource CaptionTextStyle}" />
                </StackPanel>
            </Border>

            <ListView Grid.Row="3"
                      x:Name="RootFolderList"
                      ItemsSource="{Binding ViewModel.RootFolders}"
                      SelectedItem="{Binding ViewModel.SelectedRootFolder, Mode=TwoWay}"
                      SelectionMode="Single"
                      IsItemClickEnabled="True"
                      ItemClick="OnFolderItemClick">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,10"
                                Padding="16"
                                CornerRadius="14"
                                BorderThickness="1"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                            <Grid ColumnSpacing="14">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="44" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Border Width="44" Height="44" CornerRadius="12" Background="{StaticResource AccentSurfaceBrush}">
                                    <FontIcon Glyph="&#xE8B7;" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="3">
                                    <TextBlock Text="{Binding DisplayName}" FontSize="16" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding Path}" Style="{StaticResource CaptionTextStyle}" TextTrimming="CharacterEllipsis" />
                                </StackPanel>
                                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                    <Ellipse Width="10" Height="10" Fill="{StaticResource SyncedStatusBrush}" />
                                    <FontIcon Glyph="&#xE76C;" Opacity="0.55" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.Resources>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="0" />
                    </Style>
                </ListView.Resources>
            </ListView>
        </Grid>
    </ScrollViewer>
</Page>
